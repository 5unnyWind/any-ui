import React from "react";
import { Button } from "../components";

console.log(process.env.NODE_ENV);

function App() {
  return (
    <div className="App">
      <Button></Button>
    </div>
  );
}

export default App;
